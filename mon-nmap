#!/bin/bash
# mon-nmap — Scan Nmap agressif uniquement (sC+sV+OS+traceroute)
# Étapes : [1] TCP full -> [2] Aggressive (-A) -> [3] UDP (optionnel)
# - OS au terminal + dans summary (une seule fois)
# - UDP : affiché/écrit seulement si 'udp ... open' strict (jamais open|filtered)
# - summary.txt inclut un "Résumé des services détectés (TCP)" identique à l'écran

set -euo pipefail

# === Version / Aide ==========================================================
VERSION="mon-nmap v2025.09.04-aggr-only-r2"
if [[ "${1:-}" == "--version" ]]; then
  echo "$VERSION"
  exit 0
fi
if [[ "${1:-}" == "--help" ]]; then
  cat <<EOF
$VERSION
Usage: $(basename "$0") [--udp-all] <IP_CIBLE>

Options:
  --udp-all     Scan UDP complet (-p-) au lieu du top 20
  --version     Afficher la version
  --help        Afficher cette aide
EOF
  exit 0
fi

# === Couleurs ================================================================
RED='\033[1;31m'; GREEN='\033[1;32m'; YELLOW='\033[1;33m'
CYAN='\033[1;36m'; BLUE='\033[1;34m'; BOLD='\033[1m'; NC='\033[0m'

# === Option : --udp-all ======================================================
FULL_UDP=false
if [[ "${1:-}" == "--udp-all" ]]; then
  FULL_UDP=true
  shift
fi

# === Argument : IP cible =====================================================
if [ "$#" -ne 1 ]; then
  echo -e "${RED}❌ Usage: ${BOLD}$(basename "$0") [--udp-all] <IP_CIBLE>${NC}"
  exit 1
fi
IP=$1

# === Dossiers / fichiers =====================================================
DOSSIER="nmap_$IP"
mkdir -p "$DOSSIER"
F1="$DOSSIER/1-port_scan.txt"
F2="$DOSSIER/2-aggressive_scan.txt"
F3="$DOSSIER/3-udp_scan.txt"
FSUM="$DOSSIER/summary.txt"

START_TIME=$SECONDS
echo -e "${BLUE}🔍 Initialisation du scan Nmap sur ${BOLD}$IP${NC}"

# === [1] TCP full port scan ==================================================
echo -e "${YELLOW}[1] Scan de tous les ports TCP (1-65535)...${NC}"
nmap -p- --min-rate 10000 -T4 --max-retries 3 -oN "$F1" "$IP" > /dev/null 2>&1

# Ports TCP ouverts (open strict)
PORTS=$(LC_ALL=C grep -E '^[[:space:]]*[0-9]+/tcp[[:space:]]+open([[:space:]]|$)' "$F1" \
        | cut -d'/' -f1 | tr '\n' ',' | sed 's/,$//')
if [ -z "$PORTS" ]; then
  echo -e "${RED}❌ Aucun port TCP ouvert détecté.${NC}"
  exit 1
fi
echo -e "${GREEN}✔ Ports TCP détectés : ${BOLD}$PORTS${NC}"

# === [2] Scan agressif (inclut sC + sV + OS + traceroute) ===================
echo -e "${YELLOW}[2] Scan agressif avec détection OS (-A)...${NC}"
nmap -A -T4 -p"$PORTS" --max-retries 3 -oN "$F2" "$IP" > /dev/null 2>&1

# === [3] UDP (rapide par défaut) ============================================
echo -e "${YELLOW}[3] Scan UDP ${FULL_UDP:+complet (-p-) }${FULL_UDP:-rapide (top 20 ports)}...${NC}"
if $FULL_UDP; then
  nmap -sU -p- -T4 --max-retries 3 --scan-delay 5ms --host-timeout 20m -oN "$F3" "$IP"
else
  nmap -sU --top-ports 20 -T4 --max-retries 2 --host-timeout 10m -oN "$F3" "$IP" > /dev/null 2>&1
fi
echo -e "${GREEN}✔ Scan UDP terminé.${NC}"

echo -e "${GREEN}${BOLD}✔ Tous les scans sont terminés !${NC}"

# === Résumé TCP (à l’écran) depuis l’aggressif ===============================
echo -e "\n${CYAN}${BOLD}📄 RÉSUMÉ DES SERVICES TCP DÉTECTÉS :${NC}\n"
i=1; inside_block=false
while IFS= read -r line; do
  if [[ "$line" =~ ^[0-9]+/tcp[[:space:]]+open([[:space:]]|$) ]]; then
    printf "  [#%d] " "$i"
    echo -e "$line"
    i=$((i+1))
    inside_block=true
  elif [[ "$inside_block" == true && "$line" =~ ^[\|_] ]]; then
    line=${line#|_}; line=${line#|}
    echo -e "      → ${line}"
  else
    inside_block=false
  fi
done < "$F2"

# === UDP à l’écran : seulement s'il existe au moins un vrai "udp open" ======
UDP_OPEN_ONLY="$(LC_ALL=C grep -E '^[[:space:]]*[0-9]+/udp[[:space:]]+open([[:space:]]|$)' "$F3" || true)"
if [[ -n "$UDP_OPEN_ONLY" ]]; then
  echo -e "\n${CYAN}${BOLD}📄 RÉSUMÉ DES SERVICES UDP DÉTECTÉS :${NC}\n"
  echo "$UDP_OPEN_ONLY" | nl -w2 -s'. ' | while read -r line; do
    NUM=$(echo "$line" | cut -d'.' -f1)
    printf "  [#%s] %s\n" "$NUM" "$(echo "$line" | cut -d' ' -f2-)"
  done
fi

# === Détection OS (affichée au terminal) ====================================
OS_LINE="$(grep -i 'OS details:' "$F2" | head -n1 | sed 's/OS details:[[:space:]]*//I')"
if [[ -n "$OS_LINE" ]]; then
  echo -e "\n${BLUE}${BOLD}🖥️  Détection du système d’exploitation :${NC} $OS_LINE"
else
  echo -e "\n${BLUE}🖥️  OS non identifié par Nmap.${NC}"
fi

# === SUMMARY (numérotation alignée + OS + Résumé services TCP) ===============
{
  echo "==== Résumé $IP ($(date '+%F %T')) ===="
  echo

  # [1] Ports TCP ouverts (depuis le full port scan)
  if [[ -s "$F1" ]]; then
    echo "[1-port_scan] TCP open :"
    LC_ALL=C grep -E '^[[:space:]]*[0-9]+/tcp[[:space:]]+open([[:space:]]|$)' "$F1"
    echo
  fi

  # [2] Détails depuis l’aggressif (open strict) + OS
  if [[ -s "$F2" ]]; then
    echo "[2-aggressive_scan] Détails (open) :"
    LC_ALL=C grep -E '^[[:space:]]*[0-9]+/(tcp|udp)[[:space:]]+open([[:space:]]|$)' "$F2"
    echo
    if [[ -n "$OS_LINE" ]]; then
      echo "OS: $OS_LINE"
      echo
    fi
  fi

  # [3] UDP ouverts (uniquement si 'open' strict)
  if [[ -n "$UDP_OPEN_ONLY" ]]; then
    echo "[3-udp_scan] UDP ouverts :"
    echo "$UDP_OPEN_ONLY"
    echo
  fi

  # === Résumé lisible des services détectés (TCP) ============================
  echo "Résumé des services détectés (TCP) :"
  i=1; inside_block=false
  while IFS= read -r line; do
    if [[ "$line" =~ ^[0-9]+/tcp[[:space:]]+open([[:space:]]|$) ]]; then
      printf "  [#%d] %s\n" "$i" "$line"
      i=$((i+1))
      inside_block=true
    elif [[ "$inside_block" == true && "$line" =~ ^[\|_] ]]; then
      line=${line#|_}; line=${line#|}
      printf "      → %s\n" "$line"
    else
      inside_block=false
    fi
  done < "$F2"
  echo

  # CSV utiles (open strict)
  TCP_CSV="$(LC_ALL=C grep -E '^[[:space:]]*[0-9]+/tcp[[:space:]]+open([[:space:]]|$)' "$F2" | cut -d'/' -f1 | paste -sd, - 2>/dev/null || true)"
  [[ -n "${TCP_CSV:-}" ]] && { echo "TCP open (CSV) : $TCP_CSV"; echo; }

  if [[ -n "$UDP_OPEN_ONLY" ]]; then
    UDP_CSV="$(echo "$UDP_OPEN_ONLY" | cut -d'/' -f1 | paste -sd, - 2>/dev/null || true)"
    [[ -n "${UDP_CSV:-}" ]] && { echo "UDP open (CSV) : $UDP_CSV"; echo; }
  fi
} > "$FSUM"

# === Fin =====================================================================
ELAPSED=$((SECONDS - START_TIME)); MINS=$((ELAPSED/60)); SECS=$((ELAPSED%60))
echo -e "\n${CYAN}🧾  Résumé rapide :${NC} ${BOLD}$FSUM${NC}"
echo -e "${BLUE}📁 Résultats complets : ${BOLD}$DOSSIER/${NC}"
echo -e "${GREEN}⏱️  Durée totale du scan : ${BOLD}${MINS}m ${SECS}s${NC}"
